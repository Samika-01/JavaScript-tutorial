
const fruits = ["Apple", "Banana"];
const veg = ["Carrot", "Radish"];
const junk =  ["Chips", "Cake", "Ice-cream"];
//let food = fruits.concat(veg, junk);// merge the arrays

let food = fruits.concat("Kiwi");//merge an array with the value
console.log(food);